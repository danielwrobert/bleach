var BLEACH = ( function( url, options ) {
	// Create new XHR instance
	var xhr = new XMLHttpRequest();

	// Set up optional options data to pass to server
	options = options || {};
	options.method = options.method || "GET";
	options.data = options.data || null;


} )( url, options );
